[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = [
  "python3.12 -m ensurepip --upgrade",
  "python3.12 -m pip install -r requirements.txt"
]

[start]
cmd = "python3.12 -m gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120"
